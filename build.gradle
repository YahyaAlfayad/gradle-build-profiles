group 'net.yahyaalfayad.gradle.plugins'
version '1.0-SNAPSHOT'

subprojects {

    repositories {
        jcenter()
    }

    apply plugin: 'idea'
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}